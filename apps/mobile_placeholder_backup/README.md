# Mobile App (React Native)

This folder is reserved for the Expo React Native app.

Recommended bootstrap command (from workspace root):

```bash
pnpm dlx create-expo-app apps/mobile
```

After bootstrap, wire it to shared API contracts from `packages/contracts`.
